<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/White</item>
        <item name="colorPrimaryDark">@color/White</item>
        <item name="colorAccent">@color/Black</item>
        <item name="android:textColor">@color/Black</item>
        <item name="titleTextColor">@color/Black</item>

    </style>

    <style name="AppTheme.Fullscreen" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/LightGrey</item>
        <item name="colorControlActivated">@color/Gold</item>
    </style>

    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/colorMenuSelected</item>
        <item name="tabIndicatorHeight">2dp</item>
        <item name="tabPaddingStart">-10dp</item>
        <item name="tabPaddingEnd">-10dp</item>
        <item name="tabTextAppearance">@style/MyCustomTabTextAppearance</item>
        <item name="tabSelectedTextColor">@color/colorMenuSelected</item>
    </style>
    <style name="MyCustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">10dp</item>
        <item name="android:textColor">@color/Black</item>
    </style>
    
    <style name="EditProfileEditTextStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/LightGrey</item>
        <item name="hintTextColor">@color/Grey</item>
        <item name="color">@color/Grey</item>
        <item name="maxLines">1</item>
    </style>

</resources>